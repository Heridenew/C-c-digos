
int main(){
int x, y, xpcount=0, ypcount=0, ipcount=0, pzeca=0;


scanf("%d %d", &x, &y);

for(int i=x ; i=y; i++){
        if(x % i != 0){

        for(int j = i-- ; j=i++; j++){
        if((x = (i * (i++)) && (y = (i * (i--))))  && (i == ((x + y) / 2))){
        pzeca++;
        }}}}


printf("%d", pzeca);
}