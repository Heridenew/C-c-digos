#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>


 int main(void){
	int x, y, z;
	
        while(x!=0) {scanf("%d", &x);}
		for(int i = x;){
        x == ( (pow(y,2)) - (pow(z,2)) )}
        while(x!=0) {printf("%d - %d", y, z);}
	
	 return 0;

}
 
