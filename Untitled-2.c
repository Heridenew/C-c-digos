/*
Autor: Edenn Weslley
Data: 02/02/2022
    love you, reader
*/
#include <stdio.h>
#include <math.h>

int main ()
{
     int a, b, c, d, e, f;
     float x, y;
    printf("Digite os valores a, b, c, d, e, f:\n");
    scanf("%d %d %d %d %d %d", &a, &b, &c, &d, &e, &f);
    a * x + b * y == c;
    d * x + e * y == f;
    printf("Seu número x é %.0f e seu número y é %.0f", x, y);
    system("pause");
    return 0;
}
