#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
int t, d;
d != 32;

 scanf("%d\n", &t);
 int va[t];
 
 for(int i=0; i<=t;i++){
 scanf("%c", &va[i]);}
 scanf("%d", &d);
 
 for (int j = 0; j <= t-1; j++){
    int cox = va[j] + d;
    printf("%c", cox);}

    return 0;
}