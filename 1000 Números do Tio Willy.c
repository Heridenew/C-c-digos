    #include <stdio.h>
    #include <string.h>
    #include <math.h>
    #include <stdlib.h>

    int main(){
    int vet[1000], h, g, t;

for(int s = 0; s < 1000 ; s++)
{
    
    scanf("%d ", &vet[s]);
        if(vet[s] == (-1))
    {
        break;
    }
}

    int x[g];

for(g = 0; g; g++)
{
    
    scanf("%d ", &x[g]);
        if(vet[g] == (-1))
    {
        break;
    }
}
int count[h];
for(int h = 0; h < 1000 ; h++)
{
        if(vet[h] == x[h])
    {    
        count[h]++;
    }
}
  for( int p = 0; p =  )
    printf("%d appeared %d times", x ,count);

    return 0;
    }


