#include <stdio.h>
#include <math.h>

int main(){

    int num1, num2, q , x;

    while(x!=0){ scanf("%d", &x)
     if(x == 0){ break; };

    
     num1 = (x *x);
     num2 = (x-1) * (x-1);
    q = ((x+1)/2);
     printf("%d - %d\n", num1, num2);}

     return 0;}